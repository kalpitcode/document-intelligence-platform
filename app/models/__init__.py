"""
Models Package
===============

SQLAlchemy ORM models for the Document Intelligence Platform.

No models are defined yet — this package provides the registration
point for future model modules.

All models MUST inherit from `app.core.database.Base` and use the
`UUIDMixin` and `TimestampMixin` for consistent schema.
"""

from __future__ import annotations
